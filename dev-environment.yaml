# a conda env
name: pyb2d
channels:
  - conda-forge
dependencies:
  # Build tools
  - cxx-compiler
  - c-compiler
  - cmake
  - ninja
  - scikit-build-core
  - uv
  # build deps
  - python >=3.13
  - nanobind
  # run deps
  - numpy
  # Tests
  - pytest
  - pytest-sugar
  # testbed
  - glad
  - glfw
  - imgui
  - libvulkan-headers
  - libvulkan-loader
  # utils
  - pre-commit
  # debug
  - lldb
